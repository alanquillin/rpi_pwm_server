[Unit]
Description=RPi PWM Server
Requires=network.target
After=network.target

[Service]
Type=simple
ExecStart={{ install_dir }}/.venv/bin/python  \
          {{ install_dir }}/rpi_pwm_server/server.py
Restart=on-failure

[Install]
WantedBy=multi-user.target